#!/bin/sh
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <postinst> `abort-remove'
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    configure)

        echo install/upgrade Doctrine
        pear channel-discover pear.symfony.com || pear channel-update pear.symfony.com 
        pear channel-discover pear.doctrine-project.org || pear channel-update pear.doctrine-project.org 
        pear install --soft --onlyreqdeps doctrine/DoctrineDBAL || pear upgrade doctrine/DoctrineDBAL

        echo install/upgrade phpseclib
        pear channel-discover phpseclib.sourceforge.net || pear channel-update phpseclib.sourceforge.net
        pear install --soft --onlyreqdeps PHP_Compat-1.6.0a3 || pear upgrade PHP_Compat-1.6.0a3
        pear install --soft --onlyreqdeps phpseclib/Crypt_RSA-0.3.1 || pear upgrade phpseclib/Crypt_RSA-0.3.1

        echo install/upgrade OpenID
        pear install --soft --onlyreqdeps Crypt_DiffieHellman-0.2.6 || pear upgrade Crypt_DiffieHellman-0.2.6
        pear install --soft --onlyreqdeps Validate-0.8.5 || pear upgrade Validate-0.8.5
        pear install --soft --onlyreqdeps Services_Yadis-0.5.1 || pear upgrade Services_Yadis-0.5.1
        pear install --soft --onlyreqdeps HTTP_OAuth-0.2.3 || pear upgrade HTTP_OAuth-0.2.3
        pear install --soft --alldeps OpenID-0.3.3 || pear upgrade OpenID-0.3.3

        echo install/upgrade Memcached
        pecl install memcached-2.0.1 || php -r "new Memcached();"
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0


